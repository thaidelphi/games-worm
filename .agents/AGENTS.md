# Agent Rules

- Every time you modify code in this workspace, you MUST run `git add`, `git commit -m "..."`, and `git push` to upload the changes to the GitHub repository automatically before completing your task.
- When writing or modifying code, you MUST add detailed comments explaining the logic in Thai or English. This helps ensure the codebase remains understandable and easy to maintain.
